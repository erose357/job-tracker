<h1><%= @category.title %></h1>

<h3>Jobs:</h3>
<ul>
  <% @category.jobs.each do |job| %>
    <li><%= link_to job.title, company_job_path(job.company.id, job.id) %></li>
  <% end %>
</ul>
<%= link_to "All Categories", categories_path %>
