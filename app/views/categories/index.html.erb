<h1>All Categories</h1>

<ul>
  <% @categories.each do |category| %>
    <li><%= category.title %> - <%= link_to "Edit", edit_category_path(category) %> - <%= link_to "Delete", category_path(category), method: "delete" %></li>
  <% end %>
</ul>
